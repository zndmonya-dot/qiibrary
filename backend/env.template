# Qiibrary Backend Environment Variables
# このファイルをコピーして .env を作成してください
# cp env.template .env

# ============================================
# セキュリティ関連（本番環境では必須）
# ============================================

# 管理者API認証トークン（本番環境では必ず設定！）
# 16文字以上のランダムな文字列を推奨
# 生成例: openssl rand -hex 32
ADMIN_TOKEN=your-admin-token-here-change-this-in-production

# JWT秘密鍵（本番環境では必ず変更！）
# 32文字以上のランダムな文字列を推奨
# 生成例: openssl rand -hex 32
SECRET_KEY=your-very-secret-key-change-this-in-production-minimum-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================
# データベース
# ============================================

# データベース接続URL
# 本番環境では強力なパスワードを使用してください
DATABASE_URL=postgresql://user:password@localhost:5432/qiibrary

# ============================================
# 外部API
# ============================================

# Qiita API トークン
# https://qiita.com/settings/tokens
QIITA_API_TOKEN=your_qiita_api_token_here

# YouTube Data API v3 キー（動画情報取得用）
# https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your_youtube_api_key_here

# Amazon Associates タグ（審査中 - 現在は未使用）
AMAZON_ASSOCIATE_TAG=your-tag-22

# ============================================
# アプリケーション設定
# ============================================

# CORS設定 - フロントエンドURL
FRONTEND_URL=http://localhost:3000

# 環境（development, staging, production）
ENVIRONMENT=development

# タイムゾーン
TIMEZONE=Asia/Tokyo

# ============================================
# オプション設定
# ============================================

# Vercelプレビュー環境ドメイン（必要に応じて設定）
# VERCEL_PREVIEW_DOMAIN=https://your-preview-url.vercel.app

# 追加の許可オリジン（カンマ区切り）
# EXTRA_ALLOWED_ORIGINS=https://example.com,https://another-domain.com

# Sentry（エラートラッキング - 本番環境推奨）
# https://sentry.io/ でプロジェクト作成後、DSNを取得
# SENTRY_DSN=https://xxx@sentry.io/xxx

# Git コミットSHA（Sentryリリース追跡用 - CI/CDで自動設定）
# GIT_COMMIT_SHA=abc123def456

# Redis（将来のキャッシュ機能用）
# REDIS_URL=redis://localhost:6379/0

